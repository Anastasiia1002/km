# Frontend (Vite)
VITE_BASE=/km/
# Absolute URL of the lead API (production).
# Locally you can override to /api/lead to use Vite middleware.
VITE_LEAD_API_URL=https://km-trade.net/api/lead

# Serverless /api/lead (Vercel / production host)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
